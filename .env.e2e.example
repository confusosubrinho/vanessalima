# Variáveis para E2E (copie para .env.e2e e preencha; não commite .env.e2e com chaves reais).
# Uso: carregue antes de rodar test:e2e (ex.: dotenv -e .env.e2e -- playwright test)

# Supabase (local ou staging remoto)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# App (deve ser ambiente de teste, nunca produção)
APP_BASE_URL=http://localhost:8080

# Admin E2E (opcional; padrão do seed:qa)
# E2E_ADMIN_EMAIL=qa-admin@example.com
# E2E_ADMIN_PASSWORD=qa-admin-e2e-secure

# Evitar rodar em produção
NODE_ENV=test
MODE=development
